export { ContentInterface, LLMMessageInterface, Tool, ToolCall, ToolCallInterface, ToolCalls, ToolInterface, Tools, } from '../../common/vector/messages';
import { ContentInterface } from '../../common/vector/messages';
/**
 * Class for managing content with tool calls.
 * @internal
 */
export declare class Content {
    private content;
    constructor(contentInterface?: ContentInterface);
    getExternal(): any;
    destroy(): void;
}
