import { AudioChunkInterface, ExternalReference } from '../common/data_types/common';
/**
 * Class for handling audio data chunks.
 * Provides a wrapper around native audio processing functionality.
 * @internal
 */
export declare class AudioChunk {
    /**
     * @internal
     */
    private external;
    /**
     * Creates a new AudioChunk instance.
     *
     * @param {AudioChunkInterface} params - Configuration parameters for the audio chunk
     */
    constructor(params: AudioChunkInterface);
    /**
     * Returns the external reference to the native audio chunk implementation.
     *
     * @returns External reference object
     */
    getExternal(): ExternalReference;
    /**
     * Cleans up resources associated with this audio chunk.
     */
    destroy(): void;
}
