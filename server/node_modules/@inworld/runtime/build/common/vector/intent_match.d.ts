import { ExternalReference } from '../data_types/common';
/**
 * Information about matched intent with a confidence score.
 *
 * Contains the name of the matched intent and its confidence score (0.0 to 1.0).
 */
export interface IntentMatchInterface {
    /** Name of the matched intent. */
    name: string;
    /** Confidence score (0.0 to 1.0). */
    score: number;
}
export declare class VectorIntentMatch {
    static toArray(external: ExternalReference): IntentMatchInterface[];
}
