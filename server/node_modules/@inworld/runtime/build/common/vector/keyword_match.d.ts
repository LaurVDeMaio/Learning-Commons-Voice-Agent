import { ExternalReference } from '../data_types/common';
/**
 * Information about a matched keyword.
 *
 * Contains the name of the keyword group that matched and the specific keyword that was found.
 */
export interface KeywordMatchInterface {
    /** Name of the keyword group that matched. */
    groupName: string;
    /** The keyword that was matched. */
    keyword: string;
}
export declare class VectorKeywordMatch {
    static toArray(external: ExternalReference): KeywordMatchInterface[];
}
