import { TTSOutputStream } from '../../stream';
import { ExternalReference } from '../common';
import { BaseData } from './base_data';
/**
 * TTS output data for graph processing.
 *
 * Wraps TTS output stream functionality for text-to-speech operations.
 */
export declare class TTSOutputData extends BaseData {
    /**
     * @internal
     */
    static fromExternal(stream: ExternalReference): TTSOutputStream;
}
