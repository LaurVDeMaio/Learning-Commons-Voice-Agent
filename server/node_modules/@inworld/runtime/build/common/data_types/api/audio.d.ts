import { AudioChunkInterface } from '../common';
import { AudioData as ExternalAudioData } from '../external/audio_data';
/**
 * API-level wrapper for audio waveform data.
 */
export declare class Audio {
    readonly data: number[];
    readonly sampleRate: number;
    constructor(chunk: AudioChunkInterface);
    /**
     * Create an API `Audio` from an external/native `AudioData` instance.
     */
    static from(external: ExternalAudioData): Audio;
    isApiAudio(item: unknown): item is Audio;
}
