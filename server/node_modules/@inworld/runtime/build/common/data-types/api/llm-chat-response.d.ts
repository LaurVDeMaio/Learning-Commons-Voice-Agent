import { ContentStream } from '../../stream';
import { LLMChatResponse } from '../LLMChatResponse';
import { ContentInterface } from '../vector/messages';
export { ContentInterface };
export declare class ApiLLMChatResponse {
    private readonly response;
    constructor(response: LLMChatResponse);
    static from(data: LLMChatResponse): ApiLLMChatResponse;
    isApiLLMChatResponse(item: any): item is ApiLLMChatResponse;
    isStreaming(): boolean;
    getContentStream(): ContentStream | null;
    getContent(): ContentInterface;
}
