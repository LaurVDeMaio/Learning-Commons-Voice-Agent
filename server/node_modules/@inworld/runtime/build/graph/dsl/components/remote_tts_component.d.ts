import { SpeechSynthesisConfigInterface } from '../../../primitives/tts';
import { Camelize } from '../constants';
import { Component as GraphConfigComponent } from '../graph_config_schema';
import { AbstractComponent, AbstractComponentProps } from './abstract_component';
/**
 * @typedef {Object} RemoteTTSComponentProps
 * @property {string} [id] - Optional explicit component identifier. Human-readable identifier would be generated if omitted.
 * @property {import('../../../primitives/tts').SpeechSynthesisConfigInterface} [synthesisConfig] - TTS synthesis config (camelCase required).
 *
 * Configuration interface for `RemoteTTSComponent` creation.
 *
 * @remarks
 * `synthesisConfig` accepts camelCase properties.
 */
interface RemoteTTSComponentProps extends AbstractComponentProps {
    /** Speech synthesis configuration */
    synthesisConfig?: Camelize<SpeechSynthesisConfigInterface>;
}
/**
 * Remote TTS (Text-to-Speech) component for audio synthesis services.
 * Provides access to external text-to-speech services to convert text input
 * into audio output with configurable voice parameters.
 *
 * @example
 * ```typescript
 * const ttsComponent = new RemoteTTSComponent({
 *   synthesisConfig: {
 *     voice: 'alloy',
 *     speed: 1.0,
 *     pitch: 0
 *   }
 * });
 * ```
 */
export declare class RemoteTTSComponent extends AbstractComponent {
    private synthesisConfig?;
    /**
     * Creates a new RemoteTTSComponent instance.
     *
     * @param {Object} props - Configuration for the remote TTS component
     * @param {string} [props.id] - Optional explicit component identifier. Human-readable identifier would be generated if omitted.
     * @param {import('../../../primitives/tts').SpeechSynthesisConfigInterface} [props.synthesisConfig] - TTS synthesis config (camelCase required)
     */
    constructor(props: RemoteTTSComponentProps);
    /**
     * Converts the remote TTS component to a graph configuration component.
     *
     * @returns The configured remote TTS component for graph execution
     */
    protected toGraphConfigComponent(): GraphConfigComponent;
}
export {};
