import { GraphExecutor } from './graph_executor';
import { NodeInterface } from './nodes/node';
import { HandleResponseFunction } from './nodes/types';
/**
 * Represents a handler for a graph execution.
 */
export declare class GraphExecutionHandler {
    /**
     * The index of the execution.
     */
    readonly index: number;
    /**
     * The graph executor associated with the execution.
     * @internal
     */
    private graphExecutor;
    /**
     * Creates a new GraphExecutionHandler instance.
     * @param index - The index of the execution
     * @param graphExecutor - The graph executor associated with the execution
     * @internal
     */
    constructor(index: number, graphExecutor: GraphExecutor);
    /**
     * Handles the response from a node.
     * @param node - The node to handle the response from
     * @returns A promise that resolves to the response from the node
     */
    handleResponse(node: NodeInterface): Promise<HandleResponseFunction>;
}
