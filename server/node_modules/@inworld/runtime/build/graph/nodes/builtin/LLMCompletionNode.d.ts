import { Node, NodePropsInterface } from '../node';
import { LLMChatNodeExecutionConfigPropsInterface } from '../NodeExecutionConfig';
/**
 * Interface for LLM Completion Node creation properties.
 */
export interface LLMCompletionNodePropsInterface extends NodePropsInterface {
    /** Execution configuration for the LLM completion node */
    executionConfig: LLMChatNodeExecutionConfigPropsInterface;
    /** External node reference (for component registry approach) */
    external?: any;
}
/**
 * Node for language model text completion in the graph.
 * Processes input through a language model to generate text completions.
 * @input {TextData} {@link TextData} - The data type that LLMCompletionNode accepts as input
 * @output {TextData | TextStreamData} {@link TextData} {@link TextStreamData} - The data type that LLMCompletionNode accepts as output
 */
export declare class LLMCompletionNode extends Node {
    private executionConfig;
    private stream;
    /**
     * Creates a new LLMCompletionNode instance.
     *
     * @param props - LLM completion node creation properties
     * @internal
     */
    constructor(props: LLMCompletionNodePropsInterface);
    /**
     * Cleans up resources associated with this LLM completion node.
     */
    destroy(): void;
}
