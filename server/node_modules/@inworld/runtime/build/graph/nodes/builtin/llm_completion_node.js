"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.LLMCompletionNode = void 0;
const expose_binary_1 = require("../../../expose_binary");
const node_1 = require("../node");
const node_execution_config_1 = require("./node_execution_config");
/**
 * Node for language model text completion in the graph.
 * Processes input through a language model to generate text completions.
 */
class LLMCompletionNode extends node_1.Node {
    /**
     * Creates a new LLMCompletionNode instance.
     *
     * @param props - LLM completion node creation properties
     * @internal
     */
    constructor(props) {
        const executionConfig = new node_execution_config_1.LLMChatNodeExecutionConfig(props.executionConfig);
        const statusOrCompletionNode = expose_binary_1.NodeCreateFunctions.createLLMCompletionNode(props.id, executionConfig.getExternal());
        if (!expose_binary_1.StatusOrSharedPtrLLMCompletionNodeFunctions.isOK(statusOrCompletionNode)) {
            throw new Error('Failed to create LLM completion node');
        }
        const llmCompletionNode = expose_binary_1.StatusOrSharedPtrLLMCompletionNodeFunctions.getValue(statusOrCompletionNode);
        try {
            super(props.id, llmCompletionNode);
            this.stream = props.executionConfig.stream;
            this.executionConfig = executionConfig;
        }
        catch (e) {
            executionConfig === null || executionConfig === void 0 ? void 0 : executionConfig.destroy();
            throw e;
        }
    }
    /**
     * Cleans up resources associated with this LLM completion node.
     */
    destroy() {
        var _a;
        (_a = this.executionConfig) === null || _a === void 0 ? void 0 : _a.destroy();
        this.executionConfig = null;
        if (this.external) {
            expose_binary_1.StatusOrSharedPtrLLMCompletionNodeFunctions.delete(this.external);
            this.external = null;
        }
        super.destroy();
    }
}
exports.LLMCompletionNode = LLMCompletionNode;
