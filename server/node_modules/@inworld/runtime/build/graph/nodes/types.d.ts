import { CustomNodeInputTypes } from '../../common/data_types/type_mapping';
export type CustomNodeTypeReference = {
    typeName: string;
};
export type CustomEdgeConditionReference = {
    id: string;
};
/**
 * TextResponse is a type that represents a text response.
 */
export type TextResponse = {
    /**
     * The text of the response.
     */
    text?: string;
    /**
     * Whether the response is done.
     */
    done: boolean;
};
/**
 * AudioResponse is a type that represents an audio response.
 */
export type AudioResponse = {
    /**
     * The text of the response.
     */
    text?: string;
    /**
     * The audio of the response.
     */
    audio?: {
        data: number[];
        sampleRate: number;
    };
    done: boolean;
};
export type GraphOutputStreamDataType = CustomNodeInputTypes;
