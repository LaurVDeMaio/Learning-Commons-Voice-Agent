import type { ExternalReference } from '../data_types/common';
/**
 * Base class for runtime data streams backed by an external/native handle.
 * Provides common lifecycle handling and access to the underlying stream.
 */
export declare abstract class BaseDataStream<_T> {
    protected stream: ExternalReference;
    protected onDestroy?: () => void;
    constructor(stream: ExternalReference, onDestroy?: () => void);
    /**
     * Returns the external reference to the native stream implementation.
     * @internal
     */
    getStream(): ExternalReference;
}
