import { ExternalReference } from './common';
/**
 * Goal progression state in conversation systems.
 *
 * Tracks activated, completed, and current goals throughout the interaction.
 */
export interface GoalAdvancementInterface {
    /** Goals that have been newly activated. */
    activatedGoals: string[];
    /** Goals that have been successfully completed. */
    completedGoals: string[];
    /** Goals that are currently being pursued. */
    currentGoals: string[];
}
/**
 * Represents a goal advancement.
 */
export declare class GoalAdvancement {
    /**
     * @internal
     */
    private external;
    /**
     * @internal
     * Constructs a new GoalAdvancement instance.
     *
     * @param {ExternalReference} external - The external reference to create the instance from
     */
    constructor(external: ExternalReference);
    /**
     * @internal
     * Creates a new GoalAdvancement instance from an external reference.
     *
     * @param {ExternalReference} external - The external reference to create the instance from
     * @returns {GoalAdvancement} A new GoalAdvancement instance
     */
    static fromExternal(external: ExternalReference): GoalAdvancement;
    /**
     * Retrieves the goals.
     *
     * @returns {GoalAdvancementInterface | undefined} An array of goal advancement, or undefined if no goal advancement is found
     */
    getGoals(): GoalAdvancementInterface;
}
