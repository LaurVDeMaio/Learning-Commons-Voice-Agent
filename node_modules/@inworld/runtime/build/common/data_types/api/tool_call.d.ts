import { ToolCallInterface } from '../../vector/messages';
import { ListToolCallData } from '../external/tool_call_data';
export { ToolCallInterface };
/**
 * API-level wrapper for a list of tool calls.
 * Used to pass tool call requests into the graph engine.
 */
export declare class ListToolCalls {
    readonly toolCalls: ToolCallInterface[];
    constructor(toolCalls: ToolCallInterface[]);
    /**
     * Convenience helper to convert to the external/native data type.
     */
    toExternal(): ListToolCallData;
}
