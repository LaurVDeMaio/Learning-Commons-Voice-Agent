import { ContentInterface, ToolCallInterface } from '../../vector/messages';
export declare class Content {
    readonly content: string;
    readonly toolCalls?: ToolCallInterface[];
    constructor(data: ContentInterface);
    static from(data: ContentInterface): Content;
    isContent(item: any): item is Content;
}
