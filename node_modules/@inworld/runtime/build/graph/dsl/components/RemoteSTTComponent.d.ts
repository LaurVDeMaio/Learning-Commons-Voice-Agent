import { Camelize } from '../constants';
import { Component as GraphConfigComponent, SpeechRecognitionConfig3 } from '../graph_config_schema';
import { AbstractComponent, AbstractComponentProps } from './AbstractComponent';
/**
 * Configuration interface for RemoteSTTComponent creation.
 */
interface RemoteSTTComponentProps extends AbstractComponentProps {
    /** STT configuration object */
    sttConfig: Camelize<SpeechRecognitionConfig3>;
}
/**
 * Remote STT (Speech-to-Text) component for audio transcription services.
 * This component provides access to external speech recognition services
 * for converting audio input into text transcriptions.
 *
 * @example
 * ```typescript
 * const sttComponent = new RemoteSTTComponent({
 *   id: 'my-stt-component',
 *   sttConfig: {
 *     provider: 'whisper',
 *     modelName: 'whisper-1',
 *     language: 'en'
 *   }
 * });
 * ```
 */
export declare class RemoteSTTComponent extends AbstractComponent {
    private sttConfig;
    /**
     * Creates a new RemoteSTTComponent instance.
     *
     * @param props - Configuration for the remote STT component
     */
    constructor(props: RemoteSTTComponentProps);
    /**
     * Converts the remote STT component to a graph configuration component.
     *
     * @returns The configured remote STT component for graph execution
     */
    protected toGraphConfigComponent(): GraphConfigComponent;
}
export {};
